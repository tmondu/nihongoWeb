# npm configuration for KanaDojo
# This helps resolve common installation issues across different platforms

# Increase timeout for slow networks
# timeout=60000

# Avoid stale package metadata during upgrades; use --prefer-offline per command
# when you specifically want faster reinstalls from cache.
prefer-offline=false

